#include<stdio.h>
#include<string.h>
int main()
{
    char s[200];
    printf("enter the string:");
    scanf("%[^\n]%*c",s);
   int i=0;
    while(s[i])
    {
        s[i]=tolower(s[i]);
        i++;
    }
    printf("%s",s);
    return 0;
}
